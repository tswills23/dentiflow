node_modules/
.env
.dev.vars
token.json
credentials.json
dist/
.wrangler/
.tmp/
*.pyc
__pycache__/
*.csv
dentiflow/
nul

# Supabase CLI cache and runtime locks
supabase/.temp/
.claude/scheduled_tasks.lock

# Auto-captured command-failure log (runtime noise; LESSONS.md is the kept layer)
.claude/failures.jsonl

# CallRail recordings — PHI (patient call audio + transcripts). NEVER commit.
callrail-puller/recordings/
scripts/.callback-sheet-id.txt
